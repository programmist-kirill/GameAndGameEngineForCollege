import keyboard
import os
import time
DIRECTORY_CACHE_KEYBOARD_WORD = "/home/kirill/game_engine_new/Cache/keyboard.word"
DIRECTORY_SCENE_DLL = "/home/kirill/NOT_DELETE_FOLDER/game_for_college/Data/scene.dll"
def main():
    while True:
        if keyboard.is_pressed('d'):    
            with open(DIRECTORY_CACHE_KEYBOARD_WORD, "w") as fp:
                fp.write("d")
            os.system(f"python {DIRECTORY_SCENE_DLL}")
            time.sleep(0.2)
        elif keyboard.is_pressed('a'):
            with open(DIRECTORY_CACHE_KEYBOARD_WORD, "w") as fp:
                fp.write("a")
            os.system(f"python {DIRECTORY_SCENE_DLL}")
            time.sleep(0.2)
        elif keyboard.is_pressed('q'):
            break
       	time.sleep(0.01)
main()
